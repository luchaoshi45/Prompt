# 定义 OR 运算符的相关知识
OR = {
    "description": "The OR operator is used in SQL to combine multiple conditions in a WHERE clause. It allows you to retrieve rows that meet any of the specified conditions.",
    "usage": "It is typically used in conjunction with the WHERE clause, like this: WHERE condition1 OR condition2 OR ...;",
    "example": {
        "description": "Example usage in a SQL query",
        "sql_query": "SELECT * FROM employees WHERE department = 'IT' OR department = 'HR';"
    }
}