# 定义 CREATE TABLE 语句的相关知识
CREATE_TABLE = {
    "description": "The CREATE TABLE statement in SQL is used to create a new table in a database.",
    "syntax": {
        "basic": "CREATE TABLE table_name ("
                 + " column1 datatype constraints,"
                 + " column2 datatype constraints,"
                 + " ...,"
                 + " PRIMARY KEY (one or more columns)"
                 + ");"
    },
    "notes": "Note: The exact syntax for column definitions, data types, and constraints may vary depending on the SQL dialect and database management system (DBMS)."
}